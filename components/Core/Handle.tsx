import { View, Text } from "react-native";
import React from "react";

const Handle = () => {
  return (
    <View className="absolute my-2 w-full bg-transparent items-center">
      <View className="w-12 h-1 bg-gray-400" />
    </View>
  );
};

export default Handle;
